{
  "name": "polarchat",
  "version": "0.2.0-beta",
  "description": "PolarChat - A privacy-first, end-to-end encrypted chat application. No tracking. No logs. No compromise.",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:desktop": "concurrently \"npm run dev:server\" \"cargo tauri dev\"",

    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",

    "start": "cd server && npm start",

    "desktop:build": "npm run build && cargo tauri build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
